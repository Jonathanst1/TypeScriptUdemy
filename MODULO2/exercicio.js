"use strict";
let numero = 25;
console.log(`Antes de ser convertido o tipo dele é ${typeof numero}`);
let transform = numero.toString();
console.log(`Depois de ser convertido  o tipo dele é ${typeof transform}`);
